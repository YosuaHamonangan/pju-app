var constanst = {
	name: "PJUManagement",
	displayName: "PJUManagement",
	// BASE_URL: process.env.NODE_ENV === "development" ? "http://10.10.10.45:3000" : "localhost",
	BASE_URL: process.env.NODE_ENV === "development" ? "http:/192.168.100.81:3000" : "localhost",
}

module.exports = constanst;